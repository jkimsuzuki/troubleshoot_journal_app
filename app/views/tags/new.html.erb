<% content_for :title, "New Tag" %>

<section class="page-header">
  <div>
    <h1>New Tag</h1>
    <p>Create a new label to organize troubleshooting issues.</p>
  </div>
</section>

<section class="form-card">
  <%= render "form", tag: @tag %>
</section>